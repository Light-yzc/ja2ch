# Changelog

## v0.21 - 2026-05-16

首个公开测试版本。当前版本仍处于早期开发阶段，主要验证截图 OCR、悬浮窗覆盖翻译和第三方 API 翻译流程。

### Added

- 支持通过悬浮按钮触发当前屏幕截图翻译。
- 支持长按悬浮按钮进入局部区域框选，只翻译选中区域。
- 支持使用 ML Kit 识别日文 OCR 文本。
- 支持 Google ML Kit 本地翻译后端。
- 支持 OpenAI-compatible 第三方 API 翻译后端。
- 支持配置第三方 API URL、Token、模型名称和自定义 Prompt。
- 支持从第三方 API 的 `/models` 接口加载模型列表。
- 支持流式翻译输出，生成过程中逐步刷新覆盖层译文。
- 支持调整悬浮译文字号。
- 支持半透明悬浮按钮和全屏翻译覆盖层。
- 添加 README，说明功能、权限、构建方式和 GitHub Release 发布流程。

### Changed

- 第三方 API 翻译改为按批次发送 OCR 片段，使用 `<SEG_数字>` 标记切分返回结果，以保留上下文。
- 优化默认翻译提示词，要求保留片段标记、减少解释性输出，并避免把人名罗马音化。
- 第三方 API 配置保存在本机 `SharedPreferences` 中，应用升级后会继续保留配置。

### Fixed

- 修复关闭翻译覆盖层后，旧的慢速 API 请求返回时可能覆盖新结果的问题。
- 关闭覆盖层时会尝试取消当前第三方 API HTTP 请求，减少无效等待和 token 消耗。
- 修复普通全屏翻译时区域参数为空导致崩溃的问题。
- 修复覆盖层销毁后复用旧 `rootView` 导致显示旧截图的问题。

### Known Issues

- OCR 分段和排版仍不稳定，复杂页面可能出现错位或遮挡。
- 第三方 API Token 当前未加密保存，请不要在不可信设备上保存敏感 token。
- 长文本翻译依赖所选模型能力，弱模型可能出现漏标记、合并片段或翻译质量不稳定。
- 悬浮按钮拖动和位置记忆仍在开发中。
- Release 自动化打包尚未配置。
